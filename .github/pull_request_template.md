## Summary

<!-- What changed and why? -->

## Scope

- [ ] Figma-first UI/static fixture work
- [ ] Shared UI/component/infrastructure work
- [ ] Docs/instructions only
- [ ] Other:

## Figma Evidence

For `figmaBacked: true` screen/state changes, include absolute artifact paths.

- Frame register rows:
- Figma screenshots:
- Actual screenshots:
- Route URL(s):
- Viewport/state:

If no Figma evidence is required, explain why:

<!-- Not applicable because... -->

## Verification

- [ ] `pnpm verify`
- [ ] `pnpm build-storybook` if Storybook changed
- [ ] Visual Playwright spec if Figma-backed screen/state changed
- [ ] `git diff --check` or `git diff --cached --check` for docs/instructions-only changes

Commands run:

```bash

```

## Phase-Scope Safety

- [ ] No Firebase imports or setup
- [ ] No `fetch`
- [ ] No route handlers
- [ ] No server actions
- [ ] No validation schema generation
- [ ] No CRUD/business state transition logic
- [ ] No optimistic UI
- [ ] No local array add/edit/delete mutation
- [ ] No submit-driven approval/rejection/confirmation/save/delete behavior

## Notes

<!-- Review notes, known gaps, or follow-up links. -->
